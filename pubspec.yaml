name: flutter_real_time_chat_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  supabase_flutter: ^2.12.2
  provider: ^6.1.5+1
  cached_network_image: ^3.4.1
  intl: ^0.20.2
  uuid: ^4.5.3
  shared_preferences: ^2.5.5
  timeago: ^3.7.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env.local
